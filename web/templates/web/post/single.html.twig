{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ page.title }}</h1>

    {% if(page.content) %}
        {{ easy_gutenberg(page.content) }}
    {% endif %}

    {{ easy_gutenberg_assets() }}
{% endblock %}